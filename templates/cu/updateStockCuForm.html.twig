
<div class="modal-content">
{{ form_start(consumptionForm) }}
    <div class="modal-header head-table">
        <h5 class="modal-title">{{ wheels.provider.name ~ ' - ' ~ wheels.ref }}</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body text-left">
        {{ form_row(consumptionForm.linearMeters) }}
        {{ form_row(consumptionForm.date) }}
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-primary">Valider</a>  
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
    </div>
{{ form_end(consumptionForm) }}
</div>